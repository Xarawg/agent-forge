# 50 — Шаблон промпта задачи (заполняет agent-forge, не модель)

Плейсхолдеры в {{фигурных}} подставляет runner. Модель получает уже заполненный текст.

---

## Задача {{task.id}}: {{task.title}}

Спека: {{task.spec_ref}} (выдержка ниже). Пакет: {{package.name}}.

### Выдержка спеки
{{spec_excerpt}}

### Релевантный канон
{{canon_excerpt}}

### Текущее состояние выполнения (Σt)
{{state}}

Здесь передаётся структурированный JSON-объект, который агент использует как свою «память».
Он содержит всё, что было накоплено за предыдущие шаги: прочитанные файлы, результаты команд,
принятые решения и т.п. Агент должен обновлять это состояние с каждым действием и возвращать
патчи в своём ответе.

### Репо-контекст (карта сущностей, AGENTS.md, сигнатуры соседей)
{{repo_context}}

### Scope (писать только сюда)
{{task.scope_paths}}

### Acceptance (должны быть зелёными)
{{task.acceptance}}

### Журнал прошлых итераций (если repair)
{{history}}

---

Работай по роли coder (prompts/20) в режиме stateful. Возвращай JSON с `state_patch` и `action`,
а после JSON — маркер завершения: DONE / BLOCKED / GAP (или для repair: DONE / STUCK / DISPUTE).
